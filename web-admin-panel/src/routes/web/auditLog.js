'use strict';
const express = require('express');
const router = express.Router();
// TODO: implement in Phase 9
router.get('/', (req, res) => res.send('Audit Log — coming soon'));
module.exports = router;
